language: node_js
node_js:
  - "stable"
env:
  - API_URL='https://mesi-react.herokuapp.com'  
cache:
  directories:
    - node_modules
script:
  - npm test
  - npm run build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: dist
  on:
    branch: mesi-client